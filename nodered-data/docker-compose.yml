version: '3.8'

services:
  nodered:
    image: nodered/node-red:latest
    container_name: nodered
    restart: always
    ports:
      - "1880:1880"
    environment:
      - TZ=Europe/Dublin
    volumes:
      - nodered-data:/data

volumes:
  nodered-data: